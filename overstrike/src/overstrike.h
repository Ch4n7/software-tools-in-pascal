#ifndef _C7_OVERSTRIKE

#include <stdbool.h>

#define TAB ('\t')
#define SPACE (' ')
#define NEWLINE ('\n')
#define BACKSPACE ('\b')
#define PLUS ('+')

#define ADVANCE SPACE
#define NO_ADVANCE PLUS 

#define MAXLINE 100

#define MAX(a,b) ((a) > (b) ? (a) : (b))

#define _C7_OVERSTRIKE
#endif
